//! Se agrega el signo de interrogacion "?"para hacer un parametro ocpcionale n typescirpt

const funOpc = (x:number, y: number = 0) => {
console.log(x, y);
}

funOpc(1);
funOpc(1,2)
export default {}
